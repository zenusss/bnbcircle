/**
 * Feature flags for Bnb Circle
 * Toggle features on/off without code changes.
 * In production, these would be fetched from a remote config or Supabase.
 */

export const isPaymentsEnabled = false;
export const isMapEnabled = false;
export const isChatEnabled = true;
export const isICalEnabled = true;
export const isAdminEnabled = true;
export const isHostSignupEnabled = true;
